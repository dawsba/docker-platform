version: "2.8"
services:
  nginx-php:
    build:
      context: ./docker
      dockerfile: alpine.armhf.Dockerfile
    image: dawsba/pi-alpine-nginx-php:1.0
    expose:
      - 80/tcp
      - 80/udp
    networks:
      - frontend
    depends_on:
      - mysql